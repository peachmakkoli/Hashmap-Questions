
def permutations?(string1, string2)
  # return false if the strings are not the same length
  # define which string is smaller/larger
  # add each char from the smaller string to the hash table
  # lookup each char in the larger string in the hash table; if it's found,
end